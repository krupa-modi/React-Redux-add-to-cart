// we use constant bcoz of when we create action we take string inside type so if you want to use without string type than use this one'

// NOTE: we use contant file in action as well as reduce

export const ADD_TO_CART = "ADD_TO_CART";

export const REMOVE_TO_CART = "REMOVE_TO_CART";
